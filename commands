parent = document.querySelector('[aria-label="Participants"]')
children=Array.from(parent.children)
children.forEach((child,index) => {console.log(child.children[0].children[1].children[0].children[0].innerText);})

function listAll() {
    pList=document.querySelector('[aria-label="Participants"]');
    let list=[]
    Array.from(pList.children).forEach((child,index) => {list.push(child.children[0].children[1].children[0].children[0].innerText);});
    return list;
}
